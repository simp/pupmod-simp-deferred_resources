---
lookup_options:
  deferred_resources::resources:
    merge:
      strategy: deep
      knockout_prefix: --
  deferred_resources::default_options:
    merge:
      strategy: deep
      knockout_prefix: --
